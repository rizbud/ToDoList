import React, { useState } from 'react'
import {
  SafeAreaView,
  StatusBar,
  TextInput,
  FlatList,
  TouchableOpacity,
  StyleSheet,
  View,
  Text
} from 'react-native'
import { useSelector, useDispatch } from 'react-redux'

const Add = (props) => {
  
}

export default Add