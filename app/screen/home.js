import React from 'react'
import {
  SafeAreaView,
  StatusBar,
  FlatList,
  TouchableOpacity,
  StyleSheet,
  View,
  Text
} from 'react-native'
import { useSelector } from 'react-redux'

const Home = (props) => {
  
}

export default Home